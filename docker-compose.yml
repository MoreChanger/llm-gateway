version: '3'
services:
  llm-gateway:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PORT}:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    environment:
      CONFIG_FILE: /app/config.yaml
