services:
  llm-gateway:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT}:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    environment:
      CONFIG_FILE: /app/config.yaml
